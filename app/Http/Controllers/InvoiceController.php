<?php

namespace App\Http\Controllers;

use App\Models\Invoice;
use Illuminate\Http\Request;

class InvoiceController extends Controller
{
    /**
     * Display a listing of the resource.
     */
    public function index()
    {
        //
    }

    /**
     * Show the form for creating a new resource.
     */
    public function create()
    {
        //
    }

    /**
     * Store a newly created resource in storage.
     */
    public function store(Request $request)
    {
        /* primeira maneira de trablhar
        if(!$request->user()->can('isAdmin', Invoice::class)){
            abort(403);
        } */

        // Segundam aneira de trabalahr e mais elegante, e enxuto
        // $this->authorize('isAdmin', Invoice::class);
        
        if ($request->user()->cannot('isAdmin', Invoice::class)) {
            abort(403);
        }
        
        
        var_dump('store invoice');
    }

    /**
     * Display the specified resource.
     */
    public function show(string $id)
    {
        //
    }

    /**
     * Show the form for editing the specified resource.
     */
    public function edit(string $id)
    {
        //
    }

    /**
     * Update the specified resource in storage.
     */
    public function update(Request $request, string $id)
    {
        //
    }

    /**
     * Remove the specified resource from storage.
     */
    public function destroy(string $id)
    {
        //
    }
}
